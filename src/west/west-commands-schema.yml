$schema: "https://json-schema.org/draft/2020-12/schema"
$id: "https://zephyrproject.org/schemas/west/commands"
title: West Commands
description: |
  TODO

type: object
properties:
  west-commands:
    type: array
    items:
      type: object
      required: [file, commands]
      properties:
        file:
          type: string
        commands:
          type: array
          items:
            type: object
            properties:
              name:
                type: string
              class:
                type: string
              help:
                type: string
